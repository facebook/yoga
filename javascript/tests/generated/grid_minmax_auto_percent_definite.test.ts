/**
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 * @generated SignedSource<<12e8cea684c6f3152973c79709c83bd1>>
 * generated by gentest/src/GentestDriver.ts from gentest/fixtures/grid_minmax_auto_percent_definite.html
 */

import { instrinsicSizeMeasureFunc } from '../tools/utils.ts'
import Yoga from 'yoga-layout';
import {
  Align,
  BoxSizing,
  Direction,
  Display,
  Edge,
  Errata,
  ExperimentalFeature,
  FlexDirection,
  GridTrackType,
  Gutter,
  Justify,
  MeasureMode,
  Overflow,
  PositionType,
  Unit,
  Wrap,
} from 'yoga-layout';

test('grid_minmax_auto_percent_definite', () => {
  const config = Yoga.Config.create();
  let root;

  try {
    root = Yoga.Node.create(config);
    root.setPositionType(PositionType.Absolute);
    root.setDisplay(Display.Grid);
    root.setWidth(100);
    const rootGridTemplateColumns = [];
    rootGridTemplateColumns.push({type: GridTrackType.Minmax, min: {type: GridTrackType.Auto}, max: {type: GridTrackType.Percent, value: 20}});
    root.setGridTemplateColumns(rootGridTemplateColumns);
    const rootGridTemplateRows = [];
    rootGridTemplateRows.push({type: GridTrackType.Points, value: 40});
    root.setGridTemplateRows(rootGridTemplateRows);

    const root_child0 = Yoga.Node.create(config);
    root.insertChild(root_child0, 0);
    root_child0.setMeasureFunc(instrinsicSizeMeasureFunc.bind({text: "HH​HH", flexDirection: FlexDirection.Column}));
    root.calculateLayout(undefined, undefined, Direction.LTR);

    expect(root.getComputedLeft()).toBe(0);
    expect(root.getComputedTop()).toBe(0);
    expect(root.getComputedWidth()).toBe(100);
    expect(root.getComputedHeight()).toBe(40);

    expect(root_child0.getComputedLeft()).toBe(0);
    expect(root_child0.getComputedTop()).toBe(0);
    expect(root_child0.getComputedWidth()).toBe(20);
    expect(root_child0.getComputedHeight()).toBe(40);

    root.calculateLayout(undefined, undefined, Direction.RTL);

    expect(root.getComputedLeft()).toBe(0);
    expect(root.getComputedTop()).toBe(0);
    expect(root.getComputedWidth()).toBe(100);
    expect(root.getComputedHeight()).toBe(40);

    expect(root_child0.getComputedLeft()).toBe(80);
    expect(root_child0.getComputedTop()).toBe(0);
    expect(root_child0.getComputedWidth()).toBe(20);
    expect(root_child0.getComputedHeight()).toBe(40);
  } finally {
    if (typeof root !== 'undefined') {
      root.freeRecursive();
    }

    config.free();
  }
});
